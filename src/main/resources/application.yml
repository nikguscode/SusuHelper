spring:
  application:
    name: SusuHelper
  datasource:
    url: jdbc:postgresql://localhost:5432/susu_helper
    username: root
    password: root
  liquibase:
    enabled: true
    change-log: classpath:db/changelog.xml

logging:
  level:
    org:
      springframework: INFO
    liquibase: INFO